---
name: comic-story-studio
description: "End-to-end skill for turning an article's argument or a feeling into a shareable comic AND rendering it. Two modes: (A) 多格叙事体 — a continuous 起-承-转-合 narrative with a cat mascot and one sustained metaphor, rendered as scene-based SVG/PNG panels with a real-world caption under every title; (B) 单图语录体 — standalone single-image quote cards (plain bg, one centered subject, punchy 2-3 line caption below, signature stamp), styled after viral workplace-venting comics. Both share a copywriting-techniques library (metaphor concretization, puns, parody of classics, absurd-logic reversal, comparison escalation, setup-punchline, rhyme, dark-humor limits) and an emotional pacing template (empathize→escalate→self-aware pivot→soft landing). Use for turning an article/feeling into a comic for 公众号/朋友圈/小红书 — script only, or finished images plus article. Trigger on \"做一组猫咪漫画\", \"创业段子图\", \"反转漫画\", \"分镜脚本\", \"把这篇文章做成漫画\", \"生成漫画配图\", \"图文并茂的文章\", \"读者看不懂/太抽象\", \"内容太干/不够有趣\", \"单图语录漫画\", or \"吐槽漫画\"."
---

# 漫画创作工作室 (Comic Story Studio) Skill

把一段说理/道理/情绪,变成一组可发布的漫画,**并且实际渲染成图片**(SVG→PNG)。
这个 skill 有两种模式,先选模式再动手:

- **模式 A · 多格叙事体**:一个隐喻世界贯穿到底的连续剧情,起承转合,适合把一篇
  说理/科普文章的论证过程画出来(来源参考:美债/稳定币科普文章的迭代)
- **模式 B · 单图语录体**:互相独立的单图语录合集,靠"主题相同+情绪曲线"串起来,
  没有连续剧情,适合吐槽/共鸣类内容(来源参考:拆解一组稳定10W+的职场吐槽漫画)

两种模式共享 `references/copywriting-techniques.md`(文案技巧库 + 情绪节奏模板)。

## 选择模式

问自己:这批内容有没有一条贯穿的"论证链条"(A发生→导致B→所以C)?

- 有明确论证链条、要把道理讲清楚 → **模式 A**
- 只是一堆互相独立的感受/吐槽点,没有先后因果 → **模式 B**
- 拿不准,或者用户原话就是"这个道理"/"这篇文章"→ A;用户原话是"吐槽"/
  "共鸣"/"骂"/"emo"→ B

---

# 模式 A · 多格叙事体

## 第一部分:写剧本

### 叙事结构公式

每张图 = 两格,每格 = 一句标题 + 一幅插画。整组图遵循这个骨架:

1. **起(设定困境)**:主角遇到一个具体的挫折/触发事件
2. **承(冲动决策)**:主角看到"别人赚钱"的表象,产生冲动的、成本很高的解决方案
   ——如果原文本身是说理/科普文而非个人故事,把"冲动决策"替换成"表面认知/
   常见误解",下一步再由配角点破真相
3. **转折点(外部声音/理性介入)**:配角引入"更谨慎的替代方案"或"真相"——这是
   全篇道理的落点,通常是一句可以单独摘出来当金句的短句
4. **验证过程**:具体到可量化的动作+数据(时间、金额、百分比、法律条文等硬事实)
5. **双结局分支(可选,增强反转感)**:好结局(测试通过→情绪高涨)或坏结局
   (自嘲式收场,幽默而非说教)

标题保持 5-10 个汉字、动词开头或结果导向。**标题连读应该是一篇极简的微小说**,
脱离插画也能读懂大概(这一条本身就是后面"清晰度"问题的第一道防线)。

### 让内容更有趣、更丰富

上面的骨架保证"讲得清楚";这一节保证"读得下去、读完还想转发"。清楚和有趣是
两件事,清楚了不代表有趣——这一节专门补有趣和信息密度。标题/台词的具体写法,
直接查 `references/copywriting-techniques.md` 的八条技巧,这里只讲结构层面。

**节奏:信息格与情绪格交替,不要格格都在讲道理**
- 纯信息格(带新事实/新数据)和纯情绪格(负责逗一下、喘口气,没有新信息也没关系)
  按 2:1 或 3:1 交替,不要连续超过三格都在"讲道理"
- 判断标准:如果读完连续三格,读者只感觉在看加了插画的 PPT,就该插一格纯情绪
  格了——哪怕这一格只是猫的一个反应镜头,没有标题字幕也可以

**幽默手法清单(每组漫画挑 2-3 种就够,别堆砌;完整写法见文案技巧库)**
- **反差萌**:概念越严肃,表现动作/画面越幼稚可爱
- **打破第四面墙**:主角猫偶尔直接扭头看镜头吐槽一句
- **损友式吐槽**:让配角之间互怼、拆台,而不是单向"老师科普学生"
- **埋伏笔+回收**:开头出现的一个道具/台词/动作,结尾在不同语境下重新出现
- **意外整活格**:讲道理讲到一半,插一格纯搞笑、无信息量的"离题"画面

**固定人设梗(跨系列的记忆点,不是单篇技巧)**
- 给猫一个固定口头禅/招牌小动作,每个系列都用一次——读者认出这个梗,会产生
  跨系列的粘性
- 拟人化配角也可以有一句"出场必说"的固定台词

**内容丰富度:不只讲道理,还要给"料"**
- **冷知识插卡**:每 3-4 格之间插一格与主线相关但独立的趣味小知识
- **对照组角色**:加一个"杠精"或"小白"配角替读者提常见质疑,主角猫来回应
- **灵魂拷问收尾**:结尾除了态度总结,单独加一格直接抛给读者的问题
- **彩蛋细节**:某一格背景里藏一个不影响主线、但细心读者能发现的小细节
- **平行小剧场**:允许一两格短暂跳出来玩一个反差风格,立刻收回主线

**全篇情绪节奏**:套用 `references/copywriting-techniques.md` 里的"共情开场→
吐槽拉满→自我意识转折→软着陆"四段式安排整组图的顺序。

### 选一个贯穿到底的隐喻

不要中途换隐喻——读者要学会一套"世界规则"并且要它一直有效。`references/
metaphor-library.md` 里有几套现成的、带角色映射表的隐喻世界可以直接套用:

- **相亲/婚恋**:适合"缺买家的资产/机构找新买家"(债券、滞销资产、并购)
- **体检/看病**:适合"系统性风险积累、早期预警被忽视"
- **升级打怪**:适合"分阶段执行的策略/计划"
- **期末考试**:适合"有截止日期的达标/评估"
- **招聘面试**:适合"多个方案/候选者中选一个"

如果都不合适,现设计一个,写完之后追加进这个文件,下次直接查表用。

### 逐格做"隐喻→现实"对照表

**在动笔画之前**,把每一格的隐喻场景对应到原文的哪个真实事实,列一行清单
(如"神秘新人主动敲门 → 稳定币开始大规模买美债")。这张表后面会变成每格标题
下面的现实字幕——提前做,不要等画完了发现读者看不懂再回头补。

### 先出文字脚本,确认后再画

**交付给用户前,先给出完整文字脚本**(每格一句标题+一句画面描述+对应现实
一行)供确认,再进入视觉稿阶段——避免画完才发现故事逻辑要改。如果用户明确说
只要脚本,做完这一步就够了。

## 第二部分:渲染成图

### 视觉设计系统

- 画布四周统一安全边距,插画元素不贴边框;每格"标题区+现实字幕区"约占高度
  20-25%,插画区占剩余
- 黑色加粗手写感中文字体(用 `Noto Sans CJK SC`, `font-weight: 900`),标题按
  字数分档定字号(≤6字最大号,7-10字降一档),避免同组图视觉重量忽大忽小
- 极简线稿风格:黑色轮廓线+米色背景,不用阴影渐变
- 固定主角人设:白猫,耳内侧+脸颊橙色斑纹;先锁定一版角色比例,后续全部复用,
  不要每格重新设计
- 配色收敛为4-6色体系(墨色线稿+米白底+一个强调色+一个辅助色),换账号可以
  换强调色/辅助色,不要随手加新颜色
- 抽象概念一律图标化/角色化,而非纯文字说明

### 渲染流程

1. **场景化构图,禁止"图标漂浮在空白纸上"**。每一格 = 背景场景(公园/政府大楼/
   商店货架/金库等)+ 2-4 个有动作的角色,而不是单个符号加一句文字。
2. **每格标题下面必须有一行现实字幕**(小号、强调色),把隐喻直接翻译成现实——
   这是解决"读者看不懂"的最有效手段。拟人化的抽象概念(债券、稳定币等)还应该
   直接在角色身上印真实名字标签(如角色身上写"US BOND"),不要只靠开头交代一次
   就要求读者记住。
3. **用共享角色库画,不要每格手写新SVG**。`scripts/comic_lib.py` 提供:
   - 猫主角 `cat(cx, cy, s, mood, pose)`——`mood` 有 neutral/happy/surprised/
     worried/smug/determined,`pose` 有 stand/run/point/wave/knock/shrug
   - 拟人化概念角色:`bond_guy`、`wallet_guy`、`gold_coin_guy`、`oil_barrel_guy`、
     `shady_guy`、`person`(通用配角,给`color`区分身份)
   - 场景道具:`tree`、`bench`、`bulletin_board`、`house_door`、
     `government_building`、`vault`、`store_shelf`、`counter`、`world_blob`、
     `pool`、`red_thread`
   - 功能元素:`speech_bubble`、`heart`、`motion_lines`、`sound_burst`、
     `cross_out`、`check_mark`、`scale_icon`、`moneybag`、`document_icon`、
     `paw_print`(藏彩蛋用的小爪印)
   - 版面系统:`title_block(..., caption=...)` 和 `build_svg(panels)`(两格一图、
     标题带+插画带的画布系统)
   - `thought_cloud`(思考气泡)不在库里,是每个项目按需从
     `references/thought_cloud_snippet.md` 复制的小函数
   - 新增角色/道具时,复制现有函数的手臂路径/道具轮廓写法,保持同一套"手绘感"
     笔触粗细(stroke-width 3.5-9 之间),不要引入新风格

4. **渲染管线**(shell):
   ```bash
   pip install cairosvg --break-system-packages   # 每个新沙盒装一次
   python3 gen_all.py    # 用 comic_lib 拼出 comic_1.svg ... comic_N.svg
   python3 -c "
   import cairosvg
   for i in range(1, N+1):
       cairosvg.svg2png(url=f'comic_{i}.svg', write_to=f'comic_{i}.png',
                         output_width=1600, output_height=2400)
   "
   ```
   字体用 `Noto Sans CJK SC`,沙盒里通常预装;新环境可以 `fc-list :lang=zh`
   确认一下。

5. **逐张肉眼检查(`view`)再交付**,不要盲发第一版:
   - [ ] 角色坐标有没有复制粘贴错(常见 bug:两格用了同一个 Y 常量,导致角色
     消失或叠在一起)
   - [ ] 文字有没有溢出/被裁切
   - [ ] 前后景层次对不对(比如角色应该站在柜台"后面"而不是柜台"上面")
   - [ ] 跨格角色比例、配色是否一致
   - [ ] 每一格都有标题+现实字幕,隐喻没有中途跑偏
   发现问题当场改代码重渲,不要留到用户反馈里才修。

### 常见踩坑(踩过的坑)

- **Y坐标复制粘贴错**导致角色"消失"或叠在错误位置——两个格子的中心常量务必
  用不同名字(如 `CY1`/`CY2`),渲染前搜一遍有没有误用。
- **Python f-string 里 `"\\n"` 是字面反斜杠加n,不是真的换行**——多行文字用
  `chr(10)` 或真正的三引号换行。
- **手臂/肢体画成一条圆滑长弧线,容易读成"被吊着""被绳子拴着"，而不是"关节
  弯曲撑着"**:凡是要表现手臂撑在某处(枕头、桌面、扶手)的姿势,用两段直线
  在关节处转折(肩→肘→手,linejoin="round"),而不是一条 Q 曲线从肩直接
  滑到手——两段折线才有"骨骼关节"的实感,一条圆弧只会显得肢体像绳子或
  气球,可以任意拉长變形。

---

# 模式 B · 单图语录体

来源参考:拆解一组阅读量稳定 10W+ 的职场吐槽漫画(每张图独立、图上文下、
落款统一、无连续剧情)。适合"吐槽/共鸣/发泄"类内容,不适合需要讲清楚论证
过程的说理文——那种情况用模式 A。

## 和模式 A 的核心区别

|          | 模式 A 多格叙事体          | 模式 B 单图语录体          |
|----------|---------------------------|---------------------------|
| 结构     | 连续剧情,起承转合          | 互相独立,靠情绪曲线串联    |
| 单张构图 | 场景+2-4个互动角色         | 纯白/浅色底,单一主体居中   |
| 文字位置 | 标题在上,现实字幕在标题下  | 图在上,文案独立在图下方    |
| 文字角色 | 标题概括+字幕翻译隐喻       | 文案本身就是唯一的梗       |
| 一致性靠 | 固定角色设定(猫的比例)     | 固定落款印章 + 固定构图比例 |
| 适合内容 | 有论证链条的说理/科普       | 吐槽/共鸣/发泄类情绪内容    |

## 写作流程

1. **收集"槽点清单"**,不是"剧情节拍"。把原文/用户想吐槽的点拆成一条条互相
   独立的感受(比如:工作占据生活、加班没有回报、想逃又不敢逃、周末的松弛感、
   对成功的自我怀疑……),每条槽点最终对应一张图,槽点之间不需要因果关系。
2. **给每个槽点找一个能画出来的具体比喻物**(这是第一位的技巧,查文案技巧库
   第1条)。先问自己"这个感受能不能变成一个实物",画不出来说明比喻还不够具体。
3. **给比喻物配一句 2-3 行的文案**,套文案技巧库的八条手法(尤其优先"设置-
   反转两段式"和"仿写经典",命中率最高),押韵或字数对仗优先。
4. **给整组图排出情绪节奏**:开头1-2张共情("这活儿是真的累"),中段强度递增
   地吐槽,中后段插一张"自我意识转折"(预判读者被劝退,自己接住),结尾1-3张
   软着陆(自嘲但不颓、不喊口号)。
5. 和模式 A 一样,**先出文字清单(比喻物+文案)给用户确认**,再进入渲染。

## 视觉规则

单图语录体现在有两种版式,按内容感觉选,不要混用在同一组图里:

**版式一·纯白语录卡**(原版,适合一句话就能成立的强烈吐槽/金句)
- 纯白或极浅色背景,不画场景/环境道具——主体感受本身就是全部内容
- 单一主体居中,可以是人物+一个夸张化的道具/物体组合(鸟笼、锅、胡萝卜
  钓竿等),不要多角色互动,焦点必须唯一
- 图在上、文案在下,文案居中 2-3 行、字号大、加粗,不做"标题+字幕"两层结构

**版式二·沉浸场景卡**(适合需要真实环境烘托情绪的内容,比如"深夜卧室""办公室
加班"这类场景感强的槽点)
- 插画铺满整个画布(真实环境:卧室、厨房、办公室等,含家具、光线氛围),不
  留白边
- 文字直接**压在画面上**,白色描边加粗字,顶部/底部各1行,像电影海报式的
  文字排版,不单独留白色文字区
- 场景要素("床头板+被子+床头柜台灯"这类组合)一旦画定,同一账号后续同类
  场景尽量复用同一套家具画法,不用每次重新设计

**两种版式共用的规则**
- 人物脸谱化:一套简化的长相反复复用(小眼镜/腮红/圆脸这类夸张符号),
  类似模式A锁定猫的比例——这是跨图一致性的来源
- 固定签名印章放在图片区(或整个画布)右下角,每张都在同一相对位置
- 允许比模式A更粗犷、更手绘的线条质感,但不要求高保真模仿马克笔纹理
- 黑色幽默的分寸线(文案技巧库第8条)尤其重要:意象可以很敢玩,但停留在
  荒诞卡通层面,不写实、不具体化伤害细节。**踩过的坑**:把情绪具象化的道具
  直接挂在脸/身体上(比如给眼皮挂哑铃表示困),容易画成诡异感而不是可爱感
  ——脸部表情优先用读者熟悉的卡通通用惯例(睡觉泡泡、汗滴、脸红晕、晕眩
  线),外物具象化留给道具和场景层面,不要用在脸/身体本身
- **"睡着了"和"困但醒着硬撑"是两种不同的状态,画错会让文案和画面对不上**:
  文案如果说的是"手指还在动/眼睛还盯着屏幕"这种"清醒但疲惫",就要画**睁着
  的眼睛**(哪怕是重眼皮、黑眼圈),瞳孔看向屏幕方向,不能用闭眼/睡觉泡泡这
  类"已经睡着"的符号。人物姿势要符合物理常识:"躺床上"就画平躺、露出枕头
  和被子轮廓的正常姿势,不要为了"可爱"把人裹成看不出四肢在哪的茧/毛毛虫
  形状
- 手臂/肢体画成一条圆滑长弧线容易读成"被吊着",凡是要表现撑在某处的姿势,
  用两段直线在关节处转折(肩→肘→手),才有关节的实感

## 渲染方式

`scripts/comic_lib.py` 里单图语录体专用的函数,按版式分:

**版式一·纯白语录卡**
- `stamp_mark(cx, cy, text="ESK", s=1.0)`:签名印章
- `build_quote_svg(image_content, caption_lines, signature, width, height,
  image_ratio)`:单图画布——上方 `image_ratio`(默认0.62)比例是插画区,下方是
  白底文案区,`caption_lines` 传入 2-3 行字符串列表,自动居中排版

**版式二·沉浸场景卡**
- `outlined_text(cx, cy, text, size, fill, stroke)`:白底黑边描边字,压在图上
  用,不需要单独的白色文字区也能看清
- `build_scene_card_svg(scene_content, top_lines, bottom_lines, signature,
  width, height)`:整张画布就是插画区,`top_lines`/`bottom_lines` 是压在图
  上的描边文字(各支持多行),签名印章放在画布右下角
- `bedroom_phone_scene(px, py, pw, ph)`:一个现成的"深夜卧室刷手机"完整场景
  (床头板+被子+床头柜台灯+人物单手拿手机),接收整个画布区域坐标——可以
  直接用,也可以照这个写法画其他场景(厨房、办公桌等)

`image_content`/`scene_content` 里的人物,可以复用模式A角色库里的画法(比如
`person()` 的头身比例)作为起点,再加上具体道具或场景——这些道具/场景库里
覆盖不全,需要现画,画法参照模式A"新增角色/道具"的笔触规范。

渲染管线和检查清单与模式A相同(cairosvg 转 PNG、逐张肉眼检查),额外多查:

- [ ] 每张图的主体是不是"能一眼看懂在画什么感受",而不是需要看文案才懂
- [ ] 签名印章位置、大小跨图一致
- [ ] 文案单独抽出来读,是否成立、押不押韵、是否踩了黑色幽默分寸线
- [ ] 整组图有没有走完"共情→拉满→自我意识转折→软着陆"四段情绪节奏
- [ ] 用了沉浸场景卡的,压在图上的文字有没有描边、跟背景对比度够不够
- [ ] 人物四肢数量是否合理(两只手臂/两条腿都在,不要因为只需要"一只手拿
  东西"就漏画另一只)
- [ ] 装饰性道具(挂画、摆件等)如果读不出是什么、或者挡在文字预留区域里,
  直接删掉或挪开——装饰不如干净,不要为了"氛围感"硬塞一个说不清楚的方块
- [ ] 人物是完整身体(头+躯干+四肢),不是只有一颗悬浮的头;姿势要跟这一格
  文案描述的动作对应(推、躺、跑、够、瘫等),不能每张图都用同一个站姿

---

# 第三部分:拼成图文文章(两种模式通用)

用一份 Markdown 文件把"关键点文字"和图片交替排列:

- 开头一段**背景/上下文**(1段,给不了解原文的读者铺垫)
- 模式A:每个叙事节拍一个 `##` 小节,1-2条关键点(不要照抄原文大段文字——
  细节交给图片,文字只兜底"这一步在说什么")+ 对应图片
- 模式B:通常不需要逐图小节,一段引言+图片依次排列即可,文案本身已经是
  完整的表达,不需要额外总结
- 结尾一句总结/态度,呼应账号原本的语气
- 检查:文章里引用的每张图片文件名,都要和实际输出的文件名对得上

---

# 质量检查清单(发布前过一遍)

**通用**
- [ ] 图文文章的关键点够短(图片负责细节,文字不重复),引用的图片文件名和
  实际文件一致
- [ ] 全篇情绪节奏走完了"共情→拉满→自我意识转折→软着陆"四段式

**模式A专属**
- [ ] 每一格都有标题 + 现实字幕
- [ ] 每个拟人化的抽象概念,身上至少有一处直接标注真实名字
- [ ] 没有"图标漂浮在空白纸上"的格子,每格都有场景
- [ ] 全系列隐喻统一,没有中途切换世界观
- [ ] 没有连续三格以上都是纯信息格;至少用了 2-3 种幽默手法

**模式B专属**
- [ ] 每张图主体唯一、居中,没有多角色互动
- [ ] 签名印章位置、大小跨图一致
- [ ] 每句文案单独读也成立,命中至少1-2条文案技巧
- [ ] 没有写实化的自伤描述,黑色幽默停留在荒诞卡通层面

**两种模式都要做**
- [ ] 每张 PNG 都肉眼看过一遍,没有缺失元素/文字裁切/前后景错误

---

# 新创意(暂不默认执行,主动提出给用户选)

延伸方向,增加了 scope,先问用户要不要再做:

1. **角色对照表彩蛋页**:片尾或片头加一张"猫图鉴",把隐喻角色→真实实体列成
   一张一览表,作为逐格字幕之外的第二道保险。(模式A)
2. **隐喻库持续扩充**(`references/metaphor-library.md`):新隐喻用完顺手写
   进去,下次是查表不是重新设计。(模式A)
3. **长图版本**:把所有格子/图片拼进一张连续的长图(小红书/朋友圈"长图"
   格式),同一批内容多一种分享形态。(两种模式都适用)
4. **关键数字配真实小图表**:遇到"53%"这类核心数字,除了大字号,旁边配一个
   按比例画的迷你饼图/柱状图,给理性读者更精确的量感。(模式A)
5. **缩略图联系表(contact sheet)做终审**:发布前先合成一张全系列缩略图网格,
   一眼检查角色/配色是否跨图一致,而不是逐张分别看。(两种模式都适用)
6. **双语字幕模式**:面向出海/双语读者账号,现实字幕/文案用"中文 / 简短英文"
   双行。(两种模式都适用)
7. **字幕难度分档**:提供"精简版"和"新手解释版"两种现实字幕,问用户读者是
   什么水平。(模式A)
8. **角色设定表存成 assets 资产**:把猫(模式A)或打工人脸谱(模式B)的正面/
   侧面/几种表情固定画一张"角色设定表"存成图片资产,新系列开画前先展示这张
   表确认比例没有跑偏。
9. **生成后自动编号缩略图,方便用户反馈定位**:输出时把"图X-格Y"编号直接印在
   缩略图角落(仅供沟通用的临时版本,非最终发布图)。
10. **文末数据来源小尾注**:图文文章结尾加一行"数据来源:XX法案官网/财政部
    数据/日期",适合强调真实性的科普账号。(模式A)
11. **按平台一键出多尺寸**:除了公众号常用比例,额外导出小红书3:4或视频号
    封面16:9版本。(两种模式都适用)
12. **移动端可读性自检**:发布前自动检查标题/文案字号和对比度是否达标,标出
    不合格的格子。(两种模式都适用)
13. **道具库扩充(模式B专属)**:把这次分析出的"鸟笼""煮锅""胡萝卜钓竿"这类
    具象化比喻物,沉淀成 `scripts/comic_lib.py` 里的可复用函数,像模式A的
    场景道具一样越攒越多,下次不用现画。
